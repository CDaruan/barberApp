import React from 'react';
import { Container, Label } from '../components/gerais';

export default () => {
    return (
        <Container>
            <Label>Agenda</Label>
        </Container>
    );
}